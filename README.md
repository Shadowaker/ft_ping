A simple ping clone, this is mostly to learn and understand better net packets and utilities.

To compile just: <br>
<code>
make
</code>

To execute you will need <code>sudo</code> for now.<br>
<code>sudo ./ft_ping [flags] host

